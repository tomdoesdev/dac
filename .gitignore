# Build output. The binary is produced by "mise run build", never committed.
/bin/

# Test and coverage artifacts.
*.out
*.test

# Local project files used to exercise DAC against a development server.
/dac.json
/dac-lock.json
/dac-rewrite.cfg
/dacserve.json

.DS_Store
